/**
 * PLAN §9.3 — VerificationClaim v1. The canonical definition (`detail.thresholds`
 * required per I-15) now lives in `packages/protocol/src/schemas.ts` (T-035).
 */
export { VerificationClaimSchema as VerificationClaim } from "../../../../packages/protocol/src/schemas.ts";
export type { VerificationClaim } from "../../../../packages/protocol/src/schemas.ts";
