/**
 * PLAN §9.4 — ConsentRecord v1. The canonical definition now lives in
 * `packages/protocol/src/schemas.ts` (T-035).
 */
export { ConsentRecordSchema as ConsentRecord } from "../../../../packages/protocol/src/schemas.ts";
export type { ConsentRecord } from "../../../../packages/protocol/src/schemas.ts";
